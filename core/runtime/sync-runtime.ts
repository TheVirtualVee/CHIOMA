/**
 * MOVED — this file is no longer at this path.
 *
 * The sync runtime was relocated to:
 *   apps/webhook/lib/sync-runtime.ts
 *
 * It needs to live inside apps/webhook so it stays within that
 * package's rootDir and has access to the postgres dependency.
 *
 * This file exists only as a navigation marker and is excluded from all builds.
 */
export {};

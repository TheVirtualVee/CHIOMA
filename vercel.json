{
  "version": 2,
  "installCommand": "npm install",
  "routes": [
    { "src": "/health", "dest": "apps/webhook/api/health.ts" },
    { "src": "/webhook", "dest": "apps/webhook/api/webhook.ts" },
    { "src": "/admin/(.*)", "dest": "apps/webhook/api/admin/$1.ts" }
  ]
}

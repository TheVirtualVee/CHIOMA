# ARCHIVED: CHIOMA ARCHITECTURE SIMPLIFICATION (v1)
# 
# This file is archived and should not be used for deployment.
# CHIOMA has been collapsed into a single-runtime system (Vercel-only).
# Background workers and Render-specific services are removed from the active runtime.
#
# See: apps/webhook and core/runtime/sync-runtime.ts (now in apps/webhook/lib)
#
# services:
#   - name: chioma-worker
#     type: worker
#     ...
